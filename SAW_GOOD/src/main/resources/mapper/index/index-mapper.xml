<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="index">

	<select id="newProduct" resultType="map">
		select rownum r, a.* from (select * from newproduct order by productno desc) a where rownum between 1 and 4
	</select>
	
	<select id="newFunding" resultType="map">
		select rownum r, a.* from (select * from funding order by fdno desc) a where rownum between 1 and 4
	</select>
	
	<select id="newAuction" resultType="map">
		select rownum r, a.* from (select * from auction order by acboardno desc) a where rownum between 1 and 4
	</select>
	

</mapper>
