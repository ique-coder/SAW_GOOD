<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="funding">
	<select id="selectAllList" resultType="funding" parameterType="int">
 		SELECT * FROM (SELECT ROWNUM RNUM , A.* FROM(SELECT * FROM FUNDING)A) WHERE RNUM BETWEEN 1 AND #{numPerPage} ORDER BY FDNO DESC
 	</select>
  
</mapper>
